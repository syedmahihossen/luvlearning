#include <bits/stdc++.h>
using namespace std;
const int N = 1e9 + 10;
int n, cow;
int arr[N];
void solve()
{
    cin >> n >> cow;
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
}